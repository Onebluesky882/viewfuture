{
  "name": "@viewfuture/api",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@viewfuture/db": "workspace:*",
    "hono": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@viewfuture/config": "workspace:*",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}
